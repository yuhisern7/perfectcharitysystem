version: '3.8'

services:
  pcs-web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PCS_SECRET_KEY=${PCS_SECRET_KEY:-change-this-to-a-random-secret-key-in-production}
    restart: unless-stopped
